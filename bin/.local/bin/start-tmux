#!/bin/sh

readonly SESSION_NAME="$USER"

main() {
	if ! tmux has-session -t "$SESSION_NAME" 2>/dev/null; then
		tmux new-session -A -s "$SESSION_NAME" -d
	fi
	exec tmux attach-session -t "$SESSION_NAME"
}

main
